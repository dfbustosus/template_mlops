[mypy]
python_version = 3.11
check_untyped_defs = True
ignore_missing_imports = True
packages = mlops_template

# Map the package root so mypy treats imports from `src/` as the package
# root and avoids duplicate-module detection when being invoked from
# pre-commit or other tooling.
package_root = src
strict = False
